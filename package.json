{
  "name": "hunch-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:py": "cd packages/hunch-py && uv run pytest",
    "version:sync": "node scripts/sync-version.mjs",
    "version:check": "node scripts/version-check.mjs"
  }
}
